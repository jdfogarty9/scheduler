<html>
<head>
    <script>
        function clearRecipients() {
            // existing implementation
        }

        function selectAllAvailable() {
            const employees = document.querySelectorAll('.employee');
            const filteredEmployees = Array.from(employees).filter(employee => {
                // Assuming #employeeSearch is an input to filter employee names
                const searchTerm = document.querySelector('#employeeSearch').value.toLowerCase();
                return employee.textContent.toLowerCase().includes(searchTerm);
            });
            // Move filtered employees to recipients
            filteredEmployees.forEach(employee => {
                // Code to move employee to recipients
                // Code to remove employee from available
            });
            // Re-render logic
        }
    </script>
</head>
<body>
    <button onclick="selectAllAvailable()">Select All</button>
    <button onclick="clearRecipients()">Clear names</button>
    <div id="availableEmployees">
        <!-- Available employees will be listed here -->
    </div>
    <div id="recipients">
        <!-- Selected recipients will be listed here -->
    </div>
</body>
</html>